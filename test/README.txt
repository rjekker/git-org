To run the tests, you should install bats-core: https://github.com/bats-core/bats-core

Then, you can run the unit tests by just calling them like this:

./test-invocations.bats


Note that the tests assume that your default git branch is set to "main":

git config --global init.defaultBranch main

